# ── Node / npm ─────────────────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Tauri (Rust build) ─────────────────────────────────────
tauri-app/src-tauri/target/
tauri-app/src-tauri/gen/
# staged core copied into the app Resources (regenerated by `npm run bundle-core`)
bundle-staging/

# ── Outputs / generated ────────────────────────────────────
*.pdf
# exported render output from CLI/preview
*.out.*
# temp read-write image used by make-dmg.sh
rw.mdtool.dmg

# ── Playwright / Chromium cache ────────────────────────────
.playwright/
.playwright-cache/

# ── Editors & tooling ──────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ── OS ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
ehthumbs.db

# ── Local env / secrets ────────────────────────────────────
.env
.env.*
!.env.example
